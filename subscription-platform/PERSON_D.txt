=== FILE: PERSON_D.txt ===
# PERSON D: Frontend Maintenance Manual

## 1. YOUR ROLE
You manage the React frontend and API Gateway, tying the project together visually and connecting to backend routes.

## 2. FILES YOU OWN
- `frontend/src/App.jsx`
- `frontend/src/main.jsx`
- `frontend/src/index.css`
- `frontend/src/services/api.js`
- `frontend/src/components/ProtectedRoute.jsx`
- `frontend/src/pages/Login.jsx`
- `frontend/src/pages/Register.jsx`
- `frontend/src/pages/Content.jsx`
- `frontend/src/pages/Subscription.jsx`
- `frontend/package.json`
- `frontend/vite.config.js`
- `frontend/index.html`
- `frontend/.env.example`
- `frontend/.env`

## 3. WHAT TO DO — DAY BY DAY

### Day 1: API Gateway Setup

API Gateway is an AWS service that acts as a front door, routing HTTP requests from the browser to the correct Lambda.

Step 1:
AWS Console
→ API Gateway
→ HTTP API
→ Build
API name: `subscription-platform-api`
Click Next until Create.

Step 2:
AWS Console
→ API Gateway
→ subscription-platform-api
→ Routes
→ Create
Method: `ANY`, Path: `/{proxy+}`. Click Create.

Step 3: (Wait until Day 4 to create the `POST /subscribe` route when Person B deploys paymentLambda)

Step 4:
AWS Console
→ API Gateway
→ subscription-platform-api
→ Integrations
→ Manage integrations
→ Create
Attach `appLambda` to the `ANY /{proxy+}` route.
(You will attach paymentLambda later in Day 4)

Step 5:
AWS Console
→ API Gateway
→ subscription-platform-api
→ CORS
→ Configure
Access-Control-Allow-Origin: `*`
Access-Control-Allow-Methods: `*`
Access-Control-Allow-Headers: `Content-Type, Authorization`
Save

Step 6:
AWS Console
→ API Gateway
→ subscription-platform-api
→ Stages
Click `$default`. Copy the "Invoke URL".

**What information must be sent to teammates:**
Send the Invoke URL to your entire team immediately.

### Day 2: Frontend Local Setup

FILE:
frontend/.env

ACTION:
Set API Gateway URL

REASON:
Tells the React app where to send backend requests.

CODE:
VITE_API_URL=paste_your_invoke_url_here

Step 1: Open `frontend/.env` and paste your exact Invoke URL. Save the file.

### Day 3: Verify the API Service

FILE:
frontend/src/services/api.js

ACTION:
Verify API connection logic

REASON:
Connects React to API Gateway.

CODE:
const API_URL = import.meta.env.VITE_API_URL;
function getToken() { return localStorage.getItem("token"); }

export async function login(email, password) {
  const res = await fetch(`${API_URL}/auth/login`, {
    method: "POST", headers: { "Content-Type": "application/json" },
    body: JSON.stringify({ email, password }),
  });
  const data = await res.json();
  if (!res.ok) throw new Error(data.error);
  return data;
}

export async function subscribe(userId, email, tier) {
  const res = await fetch(`${API_URL}/subscribe`, {
    method: "POST", headers: { "Content-Type": "application/json" },
    body: JSON.stringify({ userId, email, tier }),
  });
  return await res.json();
}

### Day 4: Attach paymentLambda and Run React App

Step 1: Coordinate with Person B to ensure `paymentLambda` is deployed.
Step 2: 
AWS Console
→ API Gateway
→ subscription-platform-api
→ Routes
→ Create
Method: `POST`, Path: `/subscribe`. Click Create.
Step 3: 
AWS Console
→ API Gateway
→ subscription-platform-api
→ Integrations
→ Manage integrations
→ Create
Attach `paymentLambda` to the `POST /subscribe` route.

FILE:
frontend/src/App.jsx

ACTION:
Verify App Routing

REASON:
The main React component handling navigation.

CODE:
import { BrowserRouter, Routes, Route } from "react-router-dom";
import Login from "./pages/Login";
import Content from "./pages/Content";
import Subscription from "./pages/Subscription";
import ProtectedRoute from "./components/ProtectedRoute";

function App() {
  return (
    <BrowserRouter>
      <Routes>
        <Route path="/" element={<Login />} />
        <Route path="/content" element={<ProtectedRoute><Content /></ProtectedRoute>} />
        <Route path="/subscription" element={<ProtectedRoute><Subscription /></ProtectedRoute>} />
      </Routes>
    </BrowserRouter>
  );
}
export default App;

Step 4: Open your terminal.
Step 5: Type `cd frontend` and press Enter.
Step 6: Type `npm run dev` and press Enter.
Step 7: Open `http://localhost:5173` in your browser.

**How to test it:**
Click around the app. Try logging in.

**How to verify it worked:**
You should be able to see the login screen and navigate to the content page if auth is working.

**What information must be sent to teammates:**
Tell Person A the frontend is running and ready to test their auth routes!

=== END FILE ===
